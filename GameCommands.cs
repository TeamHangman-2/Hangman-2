namespace Hangman
{
    /// <summary>
    /// Enumeration of all possible commands in game
    /// </summary>
    public enum GameCommands
    {
        Help,
        Restart,
        Exit,
        ShowResult,
        ShowCommands,
    }
}
